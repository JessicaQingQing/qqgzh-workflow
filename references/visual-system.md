# Visual System

The fictional example uses deep teal (`#1F6F78`), mist (`#EAF4F2`), warm paper (`#F7F4ED`), and dark slate (`#24323D`). Use visuals to clarify a step, create a pause, or make an abstract idea memorable. Prefer one cover image plus only necessary supporting images.

Use descriptive alt text. Final images must use approved HTTPS WeChat image hosts. Rights records, source files, private assets, and likeness permissions remain in the private profile layer.
