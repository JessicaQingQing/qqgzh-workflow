---
name: 发前审查
description: 发前阻断式审查。文章必须通过每一项检查，不然不能发。不过不发。
---

# 发前审查

审查是一道**阻断闸门**。不通过就不能走发布步骤。你来执行这些检查——直接读 HTML，逐条对，逐条报 PASS 或 FAIL。

## 检查哪些

读 `references/audit-checklist.yaml` 拿当前完整的检查清单。总项数是动态的——运行时从这个文件算出来，不写死。

## 六大类，逐条过

| 类别 | 查什么 |
|------|--------|
| 结构 | HTML 有没有 `<article>` 或 `<body>`？有没有 `<h1>` 大标题？ |
| CSS 安全 | 有没有微信会吞掉的写法？`position: fixed/sticky`、`@import`、`<script>`、`<iframe>`、`<form>`、`<input>`、`<button>`——这些都不能有 |
| 图片 | 如果用了 `<img>`，每个 `src` 是不是以 Profile 里批准的主机名开头（默认只认 `https://mmbiz.qpic.cn/`）。一个图都没有 → 直接 PASS |
| 要点 | 必须有 `<section class="takeaway">`，里面是读者能带走的一句话 |
| 连接入口 | 必须有 `<section class="connect">`，里面是「关注公众号+私信暗号」的邀请 |
| 隐私 | 没有本地路径（`C:\...`、`/Users/...`、`/home/...`），没有 API Key 样子的字符串，没有凭据标记 |

## 怎么审

1. 打开生成的 HTML 文件
2. 逐条对照上面的类别检查，需要的话逐行看
3. 每条报 PASS 或 FAIL，加一句说明
4. 统计通过数：`审查通过：N/N` 或 `审查未通过：M/N`

## 审查结果处理

- **审查通过：N/N** ——可以往下走了。问用户确认发布。
- **审查未通过：M/N** ——告诉用户哪些没过、怎么改、必须重新审查通过才能走发布步骤。

审查通过是发布的必要条件，不是充分条件——用户还要亲自确认才能发。
