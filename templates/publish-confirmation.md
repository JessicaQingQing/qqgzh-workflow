# Draft publish confirmation

Audit status: PASS for revision `{{revision}}`.

I confirm an outward-facing draft with exact title `{{title}}` to `{{destination}}`.

This confirmation must follow the PASS for this exact revision. Earlier confirmations do not apply after a content change.
